# 代码结构交接

说明当前 Data Agent System 的主要代码结构、关键模块和实验入口。

## 1. 项目目标

当前项目是一个面向 Data Agent System 的事务处理研究原型。

核心设计是：

- 底层只提供版本化 KV 存储。
- agent runtime 负责事务语义、候选计划、并发控制策略选择、提交协议和失败重试。
- ATCC、传统 CC、混合策略、commit protocol 都作为可插拔模块接入。

当前重点是 ATCC 在 agent-style TPCC/YCSB workload 上的实验。

## 2. 顶层目录

```text
agent/          Python agent runtime、workload、evaluation 代码
core/           C++/pybind 底层版本化 KV 和并发控制内核
docs/           论文、汇报、交接文档
results/        保留的正式实验结果
scripts/        复现实验和汇总结果的脚本
tests/          单元测试和实验入口回归测试
third_party/    外部参考代码
```

## 3. Runtime 核心模块

### 3.1 `agent/runtime/transaction.py`

事务生命周期入口。

主要对象：

- `AgentTransactionManager`
- `AgentTransaction`

职责：

- 注册 KV 对象；
- 创建事务 snapshot；
- 管理 read set / candidates；
- 管理 pre-snapshot locks；
- 调用 commit protocol；
- 记录 trace；
- 接收可插拔的 `operation_policy` 和 `transaction_atcc_policy`。



### 3.2 `agent/runtime/cc_registry.py`

并发控制策略注册表。

职责：

- 注册传统 CC；
- 解析策略名；
- 把 `adaptive-op-strict` 和 `transaction-atcc-strict` 翻译成具体执行策略；
- 生成 pre-snapshot lock plan；
- 将提交反馈写回 ATCC 策略表。

重要策略名：

```text
occ
2pl-nowait
2pl-wait-die
mvcc-full
silo-full
tictoc-full
adaptive-op-strict
transaction-atcc-strict
adaptive-hybrid
```



### 3.3 `agent/runtime/atcc.py`

ATCC 策略表核心。

当前包含两类 ATCC：

- `PhaseAwareATCCModule`：操作级/阶段感知 ATCC 的动作模块。
- `TransactionAwareATCCModule`：事务级 ATCC，使用事务状态表、阶段动作、hot/cold read/write set 和 OCC fast path。

事务级 ATCC 的动作：

```text
occ
lock-hot-writes
lock-hot-read-write
lock-write-set
lock-read-write-set
```



### 3.4 `agent/runtime/adaptive.py`

操作级 ATCC 和旧 adaptive policy 的主要实现。

关键对象：

- `OperationPolicyTable`
- `OperationPolicyProfile`
- `OperationPolicyDecision`
- `OperationPolicyTelemetry`

它负责 per-operation 的 optimistic/pessimistic 判断。

### 3.5 `agent/runtime/hybrid.py`

family-level 自适应选择器。

职责：

- 在任务或任务窗口级别选择整个策略族；
- 例如 low conflict 选择 OCC，hot write 选择 ATCC，read-heavy 选择 MVCC/TicToc。
- TPCC 使用任务窗口里的 distinct order counter 数判断低冲突：达到 `tpcc_low_contention_min_distinct_order_counters` 时走 `tpcc_low_contention_strategy`，否则走 `hot_write_strategy`，可配置为 `adaptive-op-strict` 或 `transaction-atcc-strict`。



### 3.6 `agent/runtime/commit_protocol.py`

提交协议实现。

职责：

- 执行 candidate selection；
- 执行 commit；
- 处理传统 CC 结果；
- 处理 prelock scope；
- 处理刷新、重放、重试相关事件。



## 4. Workload 模块



### 4.1 `agent/workloads/base.py`

定义通用 agent workload 抽象：

- `AgentTask`
- `AgentCandidate`
- `AgentOperation`
- `AgentStage`
- `prepare_task_transaction`
- `populate_task_stage`

这是 TPCC/YCSB agent-style workload 的共同基础。

### 4.2 `agent/workloads/ycsb.py`

构造 agent-style YCSB。

对象格式：

```text
ycsb:record:{record}:field:{field}
```

支持：

- read；
- overwrite；
- hotspot_fraction；
- hotspot_access_probability；
- explore/refine/commit 阶段。



### 4.3 `agent/workloads/tpcc.py`

构造 agent-style TPCC。

当前实验核心是 NewOrder。

关键对象：

```text
tpcc:district:{w}:{d}:next_order_id
tpcc:district:{w}:{d}:orders
tpcc:stock:{w}:{item}:quantity
tpcc:stock:{w}:{item}:ytd
```

NewOrder workflow：

```text
explore: 读取前半库存
refine: 读取后半库存
commit: 分配订单号、追加订单、扣减库存、更新库存销售量
```



## 5. Evaluation 模块



### 5.1 `agent/evaluation/atcc_retry_experiment.py`

最重要的实验 runner。

用途：

- 跑 YCSB/TPCC retry-aware ATCC 对比实验；
- 支持 low/medium/high profile；
- 支持 staged execution；
- 支持 background workers；
- 支持传统 CC、操作级 ATCC、事务级 ATCC、adaptive-hybrid；
- 输出 JSON 结果。

常用策略：

```powershell
--strategies occ,2pl-nowait,2pl-wait-die,mvcc-full,silo-full,tictoc-full,adaptive-op-strict,transaction-atcc-strict
```



### 5.2 `agent/evaluation/atcc_manifest_runner.py`

按 JSON manifest 批量跑实验。

### 5.3 `agent/evaluation/atcc_policy_training.py`

训练/导出 operation-level ATCC policy artifact。

### 5.4 `agent/evaluation/atcc_profile_runner.py`

profile-level 训练和评估入口。

### 5.5 `agent/evaluation/atcc_ablation_experiment.py`

ATCC 四机制消融实验入口。

用途：

- 比较 `static`、`static+priority`、`dynamic`、`dynamic+priority` 四类机制；
- 同时覆盖操作级 ATCC 和事务级 ATCC；
- 支持动态策略先离线多轮训练，再 freeze policy 做 test seeds；
- 支持静态阈值 preset：
  - `conservative`：当前默认口径，不使用 YCSB hot key / TPCC `next_order_id` oracle；操作级 overwrite 首次在 `total_writes >= 32` 时转 pessimistic；事务级首次在 `write_set >= 64` 时锁 write set；retry 后扩大锁范围；
  - `threshold32`：保留上一版正式矩阵的静态阈值口径，操作级 wide overwrite 阈值为 12，事务级 wide write 阈值为 32，作为敏感性对照；
- 默认动态训练参数：`train_seeds=910104,910105,910106,910107,910108`，`train_rounds=4`，`train_policy_epsilon=0.05`，测试阶段默认 freeze learned policy；
- 支持 `--pretrained-artifacts` 加载 `atcc_ablation_policy_artifacts.json` 或完整 `atcc_ablation.json`，用于把耗时动态训练拆成多条 180 秒以内的命令，再用最终 artifact 做 frozen test；
- 输出 `summary.csv`、`atcc_ablation_metrics.csv`、`atcc_ablation_ratios.csv` 和 Markdown report。

8 个消融变体：

```text
op-static
op-static-priority
op-dynamic
op-dynamic-priority
tx-static
tx-static-priority
tx-dynamic
tx-dynamic-priority
```

优先级定义：

- 无优先级：`atcc_priority=0`，lock scheduler 使用 `race`；
- 有优先级：使用 capped priority，lock scheduler 使用 `bounded-priority`，`object_lock_priority_burst=2`。

常用入口：

```powershell
wsl bash -lc "cd /mnt/z/Data-Agent-System-master/cast-das && timeout 180s python3 -m agent.evaluation.atcc_ablation_experiment --workload all --profile all --static-preset conservative"
```

## 6. Scripts

当前保留的主要脚本：

```text
scripts/run_ycsb_compare.ps1
scripts/run_tpcc_compare.ps1
scripts/summarize_retry_results.py
scripts/run_atcc_ablation.ps1
```

用途：

- `run_ycsb_compare.ps1`：复现 YCSB low/medium/high 对比。
- `run_tpcc_compare.ps1`：复现 TPCC low/medium/high 对比。
- `summarize_retry_results.py`：把多个 JSON 汇总成 `summary.csv`。
- `run_atcc_ablation.ps1`：运行 ATCC 四机制消融实验，默认使用 conservative static preset、5 个 train seeds、4 轮训练和 frozen test policy。



## 7. Tests

测试目录保留所有 `.py` 回归测试。不要删除这些测试，它们覆盖 core runtime、traditional CC、ATCC、manifest runner、compare scripts 和事务级 ATCC。

本轮新增重点测试：

```text
tests/test_transaction_atcc.py
tests/test_atcc_ablation_experiment.py
```

`tests/test_transaction_atcc.py` 验证：

- 低风险事务走 OCC fast path；
- hot write commit 阶段只锁 hot write set；
- registry 能生成 `transaction-atcc-strict` 的 pre-snapshot plan；
- `AgentTransactionManager` 能注入事务级 ATCC policy；
- runner 能按 workload 选择 TPCC/YCSB 的事务级 ATCC 表。

`tests/test_atcc_ablation_experiment.py` 验证：

- conservative static preset 不使用 YCSB hot key 或 TPCC `next_order_id` oracle；
- `threshold32` preset 能复现上一版静态阈值行为；
- 动态 ablation policy artifact 能训练并在 test 阶段 freeze；
- no-priority 变体 priority 恒为 0；
- priority 变体保留 capped priority score。

推荐回归命令：

```powershell
wsl bash -lc "cd /mnt/z/Data-Agent-System-master/cast-das && python3 -m unittest tests.test_core tests.test_compare_scripts tests.test_atcc_family_policy tests.test_atcc_policy_training tests.test_atcc_profile_runner tests.test_atcc_manifest_runner tests.test_atcc_retry_experiment tests.test_atcc_ycsb_strict_tuned tests.test_traditional_cc_full tests.test_transaction_atcc -v"
```



## 8. Results 保留规则

当前 `results/` 只保留正式汇报和必要对照结果；smoke、调参、单 profile 分片、训练分片和旧未采用矩阵不纳入长期保留。

```text
results/transaction_atcc_full_20260628_13/
results/atcc_regression_after_ablation_20260701_15_official_corrected/
results/atcc_ablation_threshold32_formal_combined_20260701_15/
results/atcc_ablation_conservative_formal_combined_20260701_22/
results/atcc_family_benefit_combined_20260701_22/
```

含义：

- `transaction_atcc_full_20260628_13`：事务级 ATCC 的 YCSB/TPCC low/medium/high 完整矩阵。
- `atcc_regression_after_ablation_20260701_15_official_corrected`：新增消融入口后，对原有 ATCC 结果是否退化的回归对照。
- `atcc_ablation_threshold32_formal_combined_20260701_15`：上一版 threshold32 静态阈值下的四机制消融正式矩阵，作为敏感性对照。
- `atcc_ablation_conservative_formal_combined_20260701_22`：当前 conservative 静态阈值口径下的四机制消融正式矩阵。
- `atcc_family_benefit_combined_20260701_22`：当前 ATCC-family 相对传统 CC 的低/中/高冲突收益正式矩阵。

文档汇报目录：

```text
docs/ATCC综合汇报-2026.07.01.18/
```

其中包含：

- 两种 ATCC 对比传统 CC 的收益报告；
- 四机制消融实验报告；
- 静态阈值、动态学习、优先级设计说明。
