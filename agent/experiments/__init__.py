"""Research experiment compatibility layer.

Delivery users should start with `agent.cli.smoke` and `agent.cli.benchmark`.
The modules re-exported here keep the previous experiment code discoverable
without making it the main project entry point.
"""

